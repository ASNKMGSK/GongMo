# QA Debate Service — Microsoft Agent Framework 기반 3-페르소나 그룹채팅 토론

# Core — Microsoft Agent Framework 1.0 GA (2026-04-06 출시)
agent-framework>=1.0.0,<2.0.0

# FastAPI 서빙 (기존 server_v2 와 동일 런타임 스택)
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
# SSE — 현재 server.py 는 StreamingResponse 로 동작하지만, Phase 2 에서 keepalive/재연결이
# 필요해질 경우 EventSourceResponse 로 대체 가능하도록 사전 포함.
sse-starlette>=2.1

# LLM 백엔드 — Bedrock (기존 파이프라인과 동일)
boto3>=1.34

# 유틸
httpx>=0.27
python-dotenv>=1.0
