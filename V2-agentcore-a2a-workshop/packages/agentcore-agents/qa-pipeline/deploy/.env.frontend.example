# QA Pipeline V3 — 프론트 환경변수 템플릿 (EC2 /opt/qa-webapp/.env.production 로 복사)

# 백엔드 API base — nginx 리버스 프록시로 /api/ 전달 (같은 origin)
NEXT_PUBLIC_API_BASE_URL=/api
NEXT_PUBLIC_QA_SERVER_URL=/api

# 토론 UI 표시
NEXT_PUBLIC_DEBATE_PANEL_ENABLED=true

NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0
